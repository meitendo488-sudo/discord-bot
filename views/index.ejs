<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= botName %> Stats</title>
  <style>
    body {
      margin: 0;
      overflow: hidden;
      background: #555;
      color: white;
      font-family: sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      flex-direction: column;
    }
    .background-slideshow {
      position: fixed;
      inset: 0;
      z-index: -1;
    }
    .background-slideshow img {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      opacity: 0;
      transition: opacity 1s ease-in-out;
    }
    .background-slideshow img.active {
      opacity: 1;
    }
    .card {
      background: rgba(0, 0, 0, 0.6);
      border-radius: 20px;
      padding: 40px;
      text-align: center;
      box-shadow: 0 0 20px rgba(0,0,0,0.5);
    }
    img.avatar {
      border-radius: 50%;
      width: 80px;
      height: 80px;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="background-slideshow">
    <img src="/images/image1.png" class="bg active">
    <img src="/images/image2.png" class="bg">
    <img src="/images/image3.png" class="bg">
  </div>

  <div class="card">
    <img src="<%= botAvatar %>" alt="Bot Avatar" class="avatar">
    <h1><%= botName %>#<%= botDiscriminator %></h1>
    <h2>Lost Spirit Stats</h2>
    <p>Top 10 Players</p>
  </div>

  <script>
    const slides = document.querySelectorAll('.background-slideshow img');
    let current = 0;
    setInterval(() => {
      slides[current].classList.remove('active');
      current = (current + 1) % slides.length;
      slides[current].classList.add('active');
    }, 30000);
  </script>
</body>
</html>
